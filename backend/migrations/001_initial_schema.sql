-- Migration 001 — initial schema
-- Already applied manually via: psql -d gradeassist -f backend/src/db/schema.sql
-- This file exists only so the migration tracker knows 001 has been applied.
-- Do NOT run this file again.
SELECT 1;
